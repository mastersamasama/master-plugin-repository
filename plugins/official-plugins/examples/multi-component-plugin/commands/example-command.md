---
description: Example slash command from multi-component-plugin — demonstrates how commands are structured inside a plugin
---

# /example-command

When this command is invoked, print:

> Hello from example-command in multi-component-plugin. Slash commands are simple Markdown files in the `commands/` directory of a plugin.

That's the entire command. Real commands typically read arguments, run tools, and produce structured output.
